{
  "id": "openclaw-hootrix-trace",
  "name": "Hootrix-trace",
  "description": "Export OpenClaw LLM traces to Hootrix",
  "commandAliases": [
    {
      "name": "hootrix",
      "cliCommand": "hootrix"
    }
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean" },
      "debug": { "type": "boolean" },
      "apiKey": { "type": "string" },
      "apiUrl": { "type": "string" },
      "projectName": { "type": "string" },
      "workspaceName": { "type": "string" },
      "tags": {
        "type": "array",
        "items": { "type": "string" }
      },
      "toolResultPersistSanitizeEnabled": { "type": "boolean" },
      "staleTraceCleanupEnabled": { "type": "boolean" },
      "staleTraceTimeoutMs": { "type": "number" },
      "staleSweepIntervalMs": { "type": "number" },
      "flushRetryCount": { "type": "number" },
      "flushRetryBaseDelayMs": { "type": "number" }
    }
  },
  "uiHints": {
    "enabled": {
      "label": "Tracing Enabled",
      "help": "Enable Hootrix trace export while the plugin is loaded."
    },
    "debug": {
      "label": "Debug Mode",
      "help": "Enable debug logging to console for Hootrix trace plugin."
    },
    "apiKey": {
      "label": "Hootrix API Key",
      "placeholder": "your-api-key",
      "sensitive": true,
      "help": "Hootrix API Key. Optional for local deployments; defaults to HOOTRIX_API_KEY."
    },
    "apiUrl": {
      "label": "Hootrix Collector API base URL",
      "placeholder": "http://127.0.0.1:9823/",
      "help": "Hootrix Collector API base URL. Defaults to HOOTRIX_URL."
    },
    "projectName": {
      "label": "Project Name",
      "placeholder": "openclaw"
    },
    "workspaceName": {
      "label": "Workspace Name",
      "placeholder": "default"
    },
    "tags": {
      "label": "Default Tags",
      "help": "List of tags applied to every created trace."
    },
    "toolResultPersistSanitizeEnabled": {
      "label": "Sanitize Persisted Tool Messages",
      "help": "When enabled, tool_result_persist rewrites local image refs in persisted tool transcript messages. Disabled by default."
    },
    "staleTraceCleanupEnabled": {
      "label": "Stale Cleanup Enabled",
      "help": "When enabled, traces inactive longer than the timeout are force-closed."
    },
    "staleTraceTimeoutMs": {
      "label": "Stale Timeout (ms)",
      "placeholder": "300000",
      "help": "Inactivity window before stale traces are force-closed. Default is 300000."
    },
    "staleSweepIntervalMs": {
      "label": "Stale Sweep Interval (ms)",
      "placeholder": "60000",
      "help": "How often stale traces are scanned. Default is 60000."
    },
    "flushRetryCount": {
      "label": "Flush Retry Count",
      "placeholder": "2",
      "help": "Number of retry attempts after a failed flush. Default is 2."
    },
    "flushRetryBaseDelayMs": {
      "label": "Flush Retry Base Delay (ms)",
      "placeholder": "250",
      "help": "Base delay for exponential flush retry backoff. Default is 250."
    }
  }
}
